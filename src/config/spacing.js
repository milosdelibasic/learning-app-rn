import {moderateScale} from './fonts';

// Border radius
export const borderRadius = {
  base: moderateScale(4),
  small: moderateScale(2),
  large: moderateScale(8),
  big: moderateScale(16),
};

// Line height
export const lineHeight = {
  base: moderateScale(12),
  small: moderateScale(9),
  large: moderateScale(16),
  big: moderateScale(36),
};

// Padding
export const padding = {
  base: moderateScale(12),
  small: moderateScale(9),
  large: moderateScale(16),
  big: moderateScale(26),
};

// Margin
export const margin = {
  base: moderateScale(12),
  small: moderateScale(9),
  large: moderateScale(16),
  big: moderateScale(26),
};

export default {
  borderRadius,
  lineHeight,
  padding,
  margin,
};
